#!/bin/bash
cmusdata=`cmus-remote -Q`
if [[ $? -eq 0 ]]; then
	exec xfce4-terminal -e 'screen -x cmus' --role termcmusplayer
else
	exec xfce4-terminal -e 'screen -R cmus -m cmus' --role termcmusplayer
fi
