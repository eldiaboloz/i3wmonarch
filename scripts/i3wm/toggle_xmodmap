#!/bin/bash
current=`xmodmap -pke | grep "keycode   9 = Escape"`
if [ $? -ne 0 ]; then
# alternate conig is loaded
	xmodmap ~/.Xmodmap
else
# default config is loaded
	xmodmap ~/.XmodmapFnEscape
fi
pkill -RTMIN+1 i3blocks
